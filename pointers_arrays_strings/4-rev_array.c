#include "main.h"
#include <stdio.h>

/**
 *reverse_array- rev array
 *@a: array
 *@n: anything
 *Return: always success
 */

void reverse_array(int *a, int n)
{
	int i;
	int j = 0;

	for (i = 0; i < n; i++)
	{
		j = i - 1;
		j--;
	}
}
