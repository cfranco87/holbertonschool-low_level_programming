#include <stdio.h>
/**
 * main - prints 
 * Return: always 0
 */

Printf("with proper grammar, but the outcome is a piece of art");
{
	char string[] = "with proper grammar, but the outcome is a piece of art"	printf("%d|n", string);
}	
