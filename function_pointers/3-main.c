#include "3-calc.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *calc - calculator
 *@operator: calls functions 
 *@num1: number 1
 *@num2: number 2
 *Return: completed operation
 */

main calc(char* operator, int num1, int num2)
{
	char op[] =&op_function
	int i = 0, j = 0;
	int c; 

	if (i = 0; i < num1; i++)
	{
		return (i++);
	}

	if (j = 0; j < num2; j++)
	{
		return (j++);
	}

	int c (op)
		char op[] =
		int i = atoi(op);
		int j = atoi(op);











	if (!operator)
	{
		_putchar ("Error\n")
		exit(99);
	}
	
	return (c);
}	

