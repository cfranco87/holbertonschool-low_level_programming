#include <stdio.h>

/**
 * main- base
 * Return: always (success)
 */

int main(void)
{
	int x;

	{
		for (x = 0; x++;)
		{
			putchar(x + '0');
			putchar (tolower(x));
		}
	}

		putchar('\n');
		return (0);
}
