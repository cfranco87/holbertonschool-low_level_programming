#include <stdio.h>

/**
 * main- base
 * Return: always (success)
 */

int main(void)
{
	int x;

	{
		for (x = 0, x <= 16, x++)
		{ 
			putchar(x);
			putchar $((10#16));
		}
	}

		putchar('\n');
		return (0);
}
